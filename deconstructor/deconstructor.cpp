#include <iostream>
#include "Sally.h"

using namespace std;

// deconstructor that runs automatically on the
// destruction of the object
int main ()
{
    Sally so;
    // dont need to call deconstructor. 
    // its called automatically.
}
